/** Maximum length for an invite label. */
export const INVITE_LABEL_MAX = 100;

/** Default maximum number of times an invite can be used. */
export const DEFAULT_MAX_USES = 1;

/** Maximum allowed value for maxUses. */
export const MAX_USES_LIMIT = 100;

/** Default invite expiry in days. */
export const INVITE_DEFAULT_EXPIRY_DAYS = 7;

/** Maximum invite expiry in days. */
export const INVITE_MAX_EXPIRY_DAYS = 30;
